{
  "name": "yatramind",
  "version": "1.0.0",
  "description": "YatraMind Travel Super App - Root Environment",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"node backend/index.js\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
